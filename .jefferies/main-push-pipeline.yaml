name: Tube Main Push Pipeline

on:
  push:
    branches:
      - main

nodes:
  - name: Build Image
    type: build
    checkout: true
    config:
      containerfile: Containerfile
      tags:
        - quay.io/the-conn/tube
      build_args:
        - RUST_TAG=1.95-slim
